# Fidoo Driver - Environment Variables Template
#
# Copy this file to .env and fill in your actual values.
# IMPORTANT: Never commit the real .env file with credentials to version control!
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your actual credentials
#   # Load in Python: from fidoo import FidooDriver
#                     client = FidooDriver.from_env()

# ============================================================================
# REQUIRED - API Authentication
# ============================================================================

# Your Fidoo API Key
# Get this from: Fidoo Settings → Company → API Key Management
# KEEP THIS SECURE! Do not share or commit to version control.
FIDOO_API_KEY=your_api_key_here

# ============================================================================
# OPTIONAL - API Configuration
# ============================================================================

# API Base URL (defaults to production if not set)
# Production: https://api.fidoo.com/v2
# Demo/Testing: https://api-demo.fidoo.com/v2
FIDOO_BASE_URL=https://api.fidoo.com/v2

# Request timeout in seconds (default: 30)
# Increase for large dataset queries or slow connections
FIDOO_TIMEOUT=30

# Maximum retry attempts for failed requests (default: 3)
# Used for automatic exponential backoff on rate limits
FIDOO_MAX_RETRIES=3

# ============================================================================
# OPTIONAL - Debug & Logging
# ============================================================================

# Enable debug logging (default: false)
# Shows all API calls, payloads, and response details
# Useful for troubleshooting integration issues
FIDOO_DEBUG=false

# ============================================================================
# OPTIONAL - Application Settings
# ============================================================================

# Your application name (optional, for logging/identification)
APP_NAME=MyFidooIntegration

# Your environment (development, staging, production)
ENVIRONMENT=development

# ============================================================================
# SECURITY NOTES
# ============================================================================
#
# 1. NEVER commit .env to version control
#    - Add .env to .gitignore
#    - Check if .env.example exists (this file) before committing
#
# 2. Rotate API keys periodically
#    - Generate new keys in Fidoo settings
#    - Update FIDOO_API_KEY
#    - Delete old keys after verification
#
# 3. Use different keys for different environments
#    - Production key: High security, limited scope
#    - Development key: Can have broader testing scope
#
# 4. Use read-only API keys when possible
#    - Set permissions to "read-only" in Fidoo
#    - Minimizes damage if key is compromised
#
# 5. Monitor API usage
#    - Check rate limits: 6,000 requests per day
#    - Monitor access logs for unusual activity
#
# ============================================================================
# TROUBLESHOOTING
# ============================================================================
#
# "Missing API key" error:
#   → Verify FIDOO_API_KEY is set in .env
#   → Restart your application after updating .env
#
# "Invalid API key" error:
#   → Check that FIDOO_API_KEY value is correct
#   → Verify API key hasn't expired or been revoked
#   → Generate a new API key if needed
#
# "Cannot reach Fidoo API" error:
#   → Verify FIDOO_BASE_URL is correct
#   → Check internet connection
#   → Verify firewall/proxy settings allow access to api.fidoo.com
#
# "Rate limit exceeded" error:
#   → Slow down API requests
#   → Use batch processing with delays
#   → Contact Fidoo for higher rate limits
#
# ============================================================================
